name: Railway AutoTest

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress run
        id: cypress
        uses: cypress-io/github-action@v6
        with:
          # Если package.json лежит сразу в корне репозитория, 
          # удали строку working-directory или оставь пустой.
          # Если же репозиторий называется так же как папка, оставь так:
          working-directory: . 
          spec: cypress/e2e/monitoring/railway.cy.js
          browser: chrome
        env:
          # Секреты должны иметь префикс CYPRESS_
          CYPRESS_LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          CYPRESS_LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}

      - name: Extract Results
        id: extract_results
        if: always() 
        run: |
          # Файл создастся там же, где запускается Cypress
          if [ -f "offers_count.txt" ]; then
            COUNT=$(cat "offers_count.txt")
            echo "count=$COUNT" >> $GITHUB_OUTPUT
          else
            echo "count=0" >> $GITHUB_OUTPUT
          fi

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }} 
          format: markdown
          message: |
            ${{ github.event.inputs.user_chat_id != '' && '👤 **Manual Testing**' || '📢 **Hourly Testing**' }}
            
            🚂 **Railway Search Result**
            **Status:** ${{ steps.cypress.outcome == 'success' && '✅ Success' || '❌ Error' }}
            **Tickets Found:** ${{ steps.extract_results.outputs.count }}
            **Time:** ${{ github.event_name == 'schedule' && 'Hourly ⏰' || 'Manual 🔘' }}