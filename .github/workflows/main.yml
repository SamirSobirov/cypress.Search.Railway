name: Railway AutoTest

on:
  schedule:
    - cron: '0 * * * *' # Запуск каждый час
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress run
        id: cypress_step
        uses: cypress-io/github-action@v6
        with:
          spec: cypress/e2e/railway.cy.js
          browser: chrome
          # Принудительная установка для предотвращения ошибки "binary missing"
          build: npx cypress install
        env:
          # В Cypress секреты из GitHub Actions должны начинаться с CYPRESS_
          CYPRESS_LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          CYPRESS_LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}

      - name: Extract Results
        if: always()
        run: |
          if [ -f offers_count.txt ]; then
            echo "OFFERS_COUNT=$(cat offers_count.txt)" >> $GITHUB_ENV
          else
            echo "OFFERS_COUNT=0" >> $GITHUB_ENV
          fi

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          message: |
            ${{ github.event.inputs.user_chat_id != '' && '👤 **Manual Testing**' || '📢 **Hourly Testing**' }}

            🚂 **Railway Search Result**
            **Status:** ${{ steps.cypress_step.outcome == 'success' && '✅ Success' || '❌ Error' }}
            **Tickets Found:** ${{ env.OFFERS_COUNT }}
            **Mode:** ${{ github.event_name == 'schedule' && 'Scheduled ⏰' || 'Manual 🔘' }}
