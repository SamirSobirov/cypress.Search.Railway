name: Railway AutoTest

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      user_chat_id:
        description: 'ID чата для личного ответа'
        required: false
        default: ''

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress run
        id: cypress
        uses: cypress-io/github-action@v6
        with:
          # Попробуй указать путь от корня репозитория. 
          # Если package.json лежит сразу в cypress.Search.Railway, пиши так:
          working-directory: cypress.Search.Railway
          spec: cypress/e2e/railway.cy.js
          browser: chrome
          # Эта команда сама установит всё нужное, если есть lock-файл
          install-command: npm ci 
        env:
          CYPRESS_LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          CYPRESS_LOGIN_PASSWORD: ${{ secrets.LOGIN_PASSWORD }}

      - name: Extract Results
        id: extract_results
        if: always() 
        run: |
          # Ищем файл именно в рабочей директории
          FILE_PATH="cypress.Search.Railway/cypress.Search.Railway/offers_count.txt"
          if [ -f "$FILE_PATH" ]; then
            COUNT=$(cat "$FILE_PATH")
            echo "count=$COUNT" >> $GITHUB_OUTPUT
          else
            echo "count=0" >> $GITHUB_OUTPUT
          fi

      - name: Send Telegram Notification
        if: always()
        uses: appleboy/telegram-action@master
        with:
          to: >-
            ${{ (github.event.inputs.user_chat_id != '') && 
            github.event.inputs.user_chat_id || 
            secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }} 
          format: markdown
          message: |
            ${{ github.event.inputs.user_chat_id != '' && '👤 **Manual Testing**' || '📢 **Hourly Testing**' }}
            
            🚂 **Railway Search Result**
            **Status:** ${{ steps.cypress.outcome == 'success' && '✅ Success' || '❌ Error' }}
            **Tickets Found:** ${{ steps.extract_results.outputs.count }}
            **Mode:** ${{ github.event_name == 'schedule' && 'Scheduled ⏰' || 'Manual 🔘' }}
            